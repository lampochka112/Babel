{
  "scripts": {
    "build": "babel src -d dist",
    "build:watch": "babel src -d dist --watch"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/cli": "^7.23.0",
    "@babel/preset-env": "^7.23.0"
  }
}